## Build Instructions

To ensure the script runs at maximum speed (under 5ms), compile the Swift source code into a binary executable using the following Terminal command:

```bash
swiftc -O toggle-chrome-sidebar.swift -o toggle-chrome-sidebar
```
